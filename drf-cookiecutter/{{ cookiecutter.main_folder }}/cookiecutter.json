{
  "project_name": "Cookiecutter Website Simple",
  "project_slug": "{{ cookiecutter.project_name.lower().replace(' ', '_') }}",
  "jwt_audience": "{{ cookiecutter.project_slug }}",
  "jwt_issuer": "{{ cookiecutter.project_slug }}",
  "_copy_without_render": [
    "*.html"
  ]
}
